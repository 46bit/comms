[package]
name = "comms"
version = "0.1.0"
authors = ["Michael Mokrysz <hi@46b.it>"]

[dependencies]
futures = "0.1"
clippy = {version = "*", optional = true}

[dev-dependencies]
quickcheck = "0.2"

[features]
default = []
dev = ["clippy"]
